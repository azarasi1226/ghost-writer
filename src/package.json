{
  "type": "module",
  "scripts": {
    "build": "esbuild main.ts --bundle --platform=node --format=esm --outfile=main.js",
    "test": "tsx -e \"import('./main.ts').then(m => m.handler())\""
  },
  "dependencies": {
    "@google/genai": "^1.41.0",
    "cheerio": "^1.2.0",
    "dotenv": "^17.3.1",
    "fetch-cookie": "^3.2.0",
    "node-fetch": "^3.3.2",
    "tough-cookie": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.12",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
